<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Posting App</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gradient-to-br from-zinc-900 to-zinc-800 text-white min-h-screen flex justify-center px-4">
    <div class="w-full max-w-2xl mt-10">

        <!-- Header -->
        <div class="flex justify-between items-center">
            <h3 class="text-3xl font-semibold">👋 Hello, <span class="text-blue-400"><%= user.name %></span></h3>
            <div class="space-x-3">
                <a class="bg-gray-500 hover:bg-gray-600 transition px-4 py-2 rounded-lg text-sm font-medium shadow-md" href="/feed">View Feed</a>
                <a class="bg-red-500 hover:bg-red-600 transition px-4 py-2 rounded-lg text-sm font-medium shadow-md" href="/logout">Logout</a>
            </div>
        </div>
        <h5 class="text-gray-400 mt-1">Start sharing your thoughts with the world.</h5>

        <!-- Post Creation Box -->
        <div class="mt-6 bg-zinc-800 p-5 rounded-lg shadow-lg border border-zinc-700 backdrop-blur-md">
            <form method="post" action="/post">
                <textarea placeholder="What's on your mind?" class="w-full p-3 outline-none resize-none bg-zinc-700/60 text-white border border-zinc-600 rounded-md focus:ring-2 focus:ring-blue-500" name="content" rows="3"></textarea>
                <button class="w-full mt-3 py-2 bg-blue-500 hover:bg-blue-600 transition text-white font-semibold rounded-md shadow-md">Create Post</button>
            </form>
        </div>

        <!-- Posts Section -->
        <div class="posts mt-10">
            <h3 class="text-zinc-400 text-lg font-semibold">Your Posts</h3>
            <div class="postcontainer mt-4 space-y-4">

                <% user.posts.reverse().forEach(function(post){ %>
                    <div class="post bg-zinc-800 p-5 rounded-lg shadow-md border border-zinc-700 backdrop-blur-lg transition transform hover:scale-[1.02] hover:border-blue-500 duration-300">
                        <div class="flex justify-between items-center">
                            <h4 class="text-blue-400 font-semibold">@<%= user.username %></h4>
                            <small class="text-gray-500"><%= post.likes.length %> Likes</small>
                        </div>
                        <p class="text-sm text-gray-300 mt-2"><%= post.content %></p>
                        <div class="flex justify-between items-center mt-3">
                            <a class="text-blue-400 hover:text-blue-500 transition font-medium" href="/like/<%= post._id %>">
                                <%= post.likes.indexOf(user._id) === -1 ? "Like" :"Unlike" %>
                            </a>
                            <a class="text-gray-500 hover:text-gray-400 transition font-medium" href="/edit/<%= post._id %>">Edit</a>
                        </div>
                    </div>
                <% }) %>

            </div>
        </div>
    </div>
</body>
</html>
