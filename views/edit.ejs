<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Post | SaaS Dashboard</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-950 text-white flex items-center justify-center min-h-screen p-6">
    <div class="w-full max-w-2xl bg-gray-900 shadow-lg rounded-xl p-8 md:p-10 lg:p-12">
        <div class="flex flex-col md:flex-row justify-between items-center mb-6">
            <h2 class="text-xl font-semibold mb-4 md:mb-0">Edit Your Post</h2>
            <a class="bg-red-500 hover:bg-red-600 transition px-4 py-2 text-sm rounded-lg" href="/logout">Logout</a>
        </div>
        <form method="post" action="/update/<%= post._id %>">
            <textarea 
                placeholder="What's on your mind?" 
                class="w-full p-4 text-gray-200 bg-gray-800 border border-gray-700 rounded-lg resize-none focus:ring-2 focus:ring-blue-500 focus:outline-none"
                name="content"
                rows="5"
            ><%= post.content %></textarea>
            <button 
                type="submit" 
                class="w-full mt-4 py-3 bg-blue-500 hover:bg-blue-600 transition text-white font-semibold rounded-lg"
            >Update Post</button>
        </form>
    </div>
</body>
</html>
