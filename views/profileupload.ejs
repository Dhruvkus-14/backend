<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Upload Profile Picture</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-950 text-white min-h-screen flex items-center justify-center px-6">
    <div class="bg-gray-900 p-8 rounded-xl shadow-lg border border-gray-800 w-full max-w-md text-center">
        <h3 class="text-2xl font-bold mb-5">Upload Profile Picture</h3>
        <form autocomplete="off" action="/upload" method="post" enctype="multipart/form-data" class="space-y-4">
            <label class="block w-full bg-gray-800 text-gray-400 text-sm py-3 px-4 rounded-lg border border-gray-700 cursor-pointer hover:border-blue-500">
                <input type="file" name="image" class="hidden" onchange="showFileName(this)">
                <span id="file-label">Choose a file...</span>
            </label>
            <button type="submit" class="w-full py-2 bg-blue-500 hover:bg-blue-600 text-white font-semibold rounded-lg shadow-md transition">
                Upload File
            </button>
        </form>
    </div>

    <script>
        function showFileName(input) {
            const fileLabel = document.getElementById("file-label");
            fileLabel.textContent = input.files.length > 0 ? input.files[0].name : "Choose a file...";
        }
    </script>
</body>
</html>
